buildscript {
    repositories {
        mavenLocal()
        maven {
            url uri('../repo')
        }
    }
    dependencies {
        classpath group: 'io.github.eja', name: 'export-functions-plugin',
				  version: '1.0-SNAPSHOT'
    }
}
apply plugin: 'export-function'
// Sponsored by Vaisala Oyj

hello()
